version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static HTML deployment - no build required"
  artifacts:
    baseDirectory: .
    files:
      - index.html
      - favicon.png
      - ogp.png
